// users

// Agents + Admins.

// Field	Type
// id	uuid (PK)
// organization_id	uuid (FK → organizations.id)
// email	varchar(255)
// name	varchar(255)
// role	varchar(50) (admin, agent, viewer)
// created_at	timestamptz
